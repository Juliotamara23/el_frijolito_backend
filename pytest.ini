[pytest]
asyncio_mode = auto
testpaths = app/tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    asyncio: mark a test as an async test
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning